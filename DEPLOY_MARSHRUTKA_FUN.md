# 🚀 Подключение marshrutka.fun к Vercel через GitHub

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка репозитория на GitHub

✅ Репозиторий уже создан: `https://github.com/elektrik5644-netizen/marhrun`

Убедитесь, что все изменения закоммичены и запушены:

```bash
git add .
git commit -m "Подготовка к деплою на Vercel"
git push origin main
```

---

### Шаг 2: Создание проекта на Vercel

1. **Откройте [vercel.com](https://vercel.com)** и войдите через GitHub

2. **Нажмите "Add New Project"** (или "New Project")

3. **Импортируйте репозиторий:**
   - Выберите `elektrik5644-netizen/marhrun`
   - Нажмите "Import"

4. **Настройте проект:**
   ```
   Framework Preset: Other
   Root Directory: server
   Build Command: npm run build
   Output Directory: dist
   Install Command: npm install
   ```

5. **Нажмите "Deploy"**

   Vercel автоматически развернет проект и даст вам URL типа `marshrutka-backend-xxx.vercel.app`

---

### Шаг 3: Настройка переменных окружения

В проекте на Vercel перейдите в **Settings → Environment Variables** и добавьте:

#### Обязательные переменные:

```env
NODE_ENV=production
JWT_SECRET=ваш_случайный_секрет_минимум_32_символа_для_jwt_токенов
```

#### База данных (выберите один вариант):

**Вариант A: Vercel Postgres (рекомендуется)**

1. В проекте перейдите в **Storage**
2. Нажмите **Create Database → Postgres**
3. Выберите регион (например, `fra1` - Frankfurt)
4. Нажмите **Create**

Vercel автоматически добавит переменные:
- `POSTGRES_URL`
- `POSTGRES_PRISMA_URL`
- `POSTGRES_URL_NON_POOLING`

Затем добавьте вручную:
```env
DATABASE_URL=$POSTGRES_URL
```

**Вариант B: Внешняя база данных (Supabase, Neon и т.д.)**

```env
DATABASE_URL=postgresql://user:password@host:5432/database
```

#### Дополнительные переменные (если используются):

```env
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_секретный_ключ
YANDEX_MAPS_API_KEY=ваш_api_ключ
```

---

### Шаг 4: Запуск миграций базы данных

После создания базы данных нужно запустить миграции:

#### Вариант A: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Подключите проект
cd server
vercel link

# Скачайте переменные окружения
vercel env pull .env.local

# Запустите миграции
npm run migrate
```

#### Вариант B: Вручную через подключение к БД

Используйте любой PostgreSQL клиент (pgAdmin, DBeaver, psql) для подключения к базе данных и выполните SQL из файлов миграций.

---

### Шаг 5: Подключение домена marshrutka.fun

1. **В проекте на Vercel перейдите в Settings → Domains**

2. **Нажмите "Add Domain"**

3. **Введите `marshrutka.fun`** и нажмите "Add"

4. **Vercel покажет DNS записи для настройки**

---

### Шаг 6: Настройка DNS у регистратора домена

Откройте панель управления вашего регистратора домена (где вы купили marshrutka.fun) и добавьте DNS записи:

#### Вариант A: Использование A записи (для корневого домена)

```
Тип: A
Имя: @ (или оставьте пустым)
Значение: 76.76.21.21
TTL: 3600 (или Auto)
```

#### Вариант B: Использование CNAME (для поддоменов)

```
Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
TTL: 3600
```

#### Вариант C: Использование Vercel Nameservers (рекомендуется)

В настройках домена у регистратора измените nameservers на:

```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

Это позволит Vercel полностью управлять DNS записями.

---

### Шаг 7: Ожидание распространения DNS

После настройки DNS записи могут распространяться до 48 часов, но обычно это занимает несколько минут.

Проверьте статус:

```bash
# Проверка DNS
nslookup marshrutka.fun

# Проверка доступности
curl https://marshrutka.fun
```

В Vercel Dashboard → Domains вы увидите статус:
- ⏳ **Pending** - DNS настраивается
- ✅ **Valid** - Домен подключен и работает

---

### Шаг 8: Обновление мобильных приложений

После того как домен заработает, обновите API URL в приложениях:

#### DriverApp

Файл: `DriverApp/src/config/api.config.ts`

```typescript
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

#### PassengerApp

Файл: `PassengerApp/src/config/api.config.ts`

```typescript
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

Затем пересоберите приложения:

```bash
# DriverApp
cd DriverApp/android
.\gradlew assembleDebug

# PassengerApp
cd PassengerApp/android
.\gradlew assembleDebug
```

---

### Шаг 9: Проверка работы

1. **Проверьте health check:**
   ```bash
   curl https://marshrutka.fun
   ```
   
   Должен вернуть:
   ```json
   {
     "status": "ok",
     "message": "Marshrutka API is running",
     "version": "1.0.0"
   }
   ```

2. **Проверьте API endpoints:**
   ```bash
   curl https://marshrutka.fun/api/v1/routes
   ```

3. **Проверьте SSL сертификат:**
   - Откройте `https://marshrutka.fun` в браузере
   - Должен быть зеленый замочек (SSL сертификат выдается автоматически)

---

## 🔄 Автоматическое развертывание

После настройки, каждый раз когда вы делаете `git push`, Vercel автоматически:
- Соберет новую версию
- Развернет на production
- Обновит домен marshrutka.fun

```bash
git add .
git commit -m "Обновление API"
git push origin main
```

---

## 📊 Мониторинг и логи

### Просмотр логов

1. В Vercel Dashboard откройте проект
2. Перейдите в **Deployments**
3. Выберите последний deployment
4. Нажмите **View Function Logs**

### Через CLI

```bash
vercel logs
```

---

## 🚨 Решение проблем

### Домен не работает

1. Проверьте DNS: `nslookup marshrutka.fun`
2. Убедитесь что DNS записи добавлены правильно
3. Подождите до 48 часов для полного распространения
4. Проверьте статус в Vercel Dashboard → Domains

### API возвращает ошибки

1. Проверьте логи в Vercel Dashboard
2. Убедитесь что все переменные окружения добавлены
3. Проверьте подключение к базе данных
4. Убедитесь что миграции запущены

### CORS ошибки

Убедитесь что в `server/src/app.ts` правильно настроен CORS:

```typescript
const corsOptions = {
  origin: process.env.NODE_ENV === 'production' 
    ? ['https://marshrutka.fun', 'https://www.marshrutka.fun', 'https://*.vercel.app']
    : '*',
  credentials: true,
  optionsSuccessStatus: 200
};
```

---

## ✅ Чеклист

- [ ] Репозиторий загружен на GitHub
- [ ] Проект создан на Vercel
- [ ] Root Directory установлен на `server`
- [ ] Переменные окружения добавлены
- [ ] База данных создана (Vercel Postgres или внешняя)
- [ ] Миграции запущены
- [ ] Домен marshrutka.fun добавлен в Vercel
- [ ] DNS записи настроены у регистратора
- [ ] Домен валидирован в Vercel
- [ ] SSL сертификат активен
- [ ] API доступен по https://marshrutka.fun
- [ ] Health check работает
- [ ] Мобильные приложения обновлены
- [ ] Приложения протестированы

---

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
- [Custom Domains](https://vercel.com/docs/concepts/projects/custom-domains)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

**🎉 Готово! Ваш сервер работает на marshrutka.fun!**

