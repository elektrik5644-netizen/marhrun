# 🚀 Развертывание системы Marshrutka на marshrutka.fun

## 📋 Содержание

1. [Настройка сервера](#настройка-сервера)
2. [Настройка DNS](#настройка-dns)
3. [Развертывание backend](#развертывание-backend)
4. [Сборка мобильных приложений](#сборка-мобильных-приложений)
5. [Тестирование](#тестирование)

---

## 🌐 Настройка DNS

### Шаг 1: Добавьте A-записи в DNS

В панели управления вашего регистратора домена (где куплен marshrutka.fun):

```
Тип    Имя              Значение           TTL
A      marshrutka.fun   ВАШ_IP_СЕРВЕРА    3600
A      www              ВАШ_IP_СЕРВЕРА    3600
```

### Шаг 2: Проверьте DNS

```bash
# Проверка DNS (может занять до 24 часов)
nslookup marshrutka.fun
ping marshrutka.fun
```

---

## 🖥️ Настройка сервера

### Требования к серверу

- **ОС**: Ubuntu 20.04/22.04 или Debian 11/12
- **RAM**: Минимум 2 GB
- **CPU**: Минимум 2 cores
- **Диск**: Минимум 20 GB SSD
- **Провайдеры**: DigitalOcean, AWS, Hetzner, или любой VPS

### Шаг 1: Подключение к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
```

### Шаг 2: Копирование файлов

**Вариант A: Через SCP**

```bash
# На локальной машине (Windows PowerShell)
scp -r server root@ВАШ_IP_АДРЕС:/var/www/marshrutka
```

**Вариант B: Через Git**

```bash
# На сервере
cd /var/www
git clone https://github.com/ВАШ_РЕПОЗИТОРИЙ/marshrutka.git
```

---

## 🚀 Развертывание Backend

### Автоматическое развертывание (рекомендуется)

```bash
cd /var/www/marshrutka/server
chmod +x deploy.sh
sudo ./deploy.sh
```

Скрипт автоматически:
- ✅ Установит все зависимости (Node.js, PostgreSQL, Nginx)
- ✅ Настроит базу данных
- ✅ Получит SSL сертификат от Let's Encrypt
- ✅ Настроит Nginx как reverse proxy
- ✅ Запустит приложение через PM2

### Настройка переменных окружения

После развертывания отредактируйте `.env`:

```bash
nano /var/www/marshrutka/.env
```

Замените:

```env
# База данных
DB_PASSWORD=СИЛЬНЫЙ_ПАРОЛЬ_123!@#

# JWT секрет (сгенерируйте случайную строку)
JWT_SECRET=ваш_случайный_секрет_минимум_32_символа

# YooKassa (для приема платежей)
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key

# Яндекс Карты
YANDEX_MAPS_API_KEY=ваш_api_key
```

### Перезапуск после изменений

```bash
sudo -u marshrutka pm2 restart marshrutka-backend
```

### Проверка работы

```bash
# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs marshrutka-backend

# Тест API
curl https://marshrutka.fun/api/v1/routes
```

---

## 📱 Сборка мобильных приложений

### DriverApp (Приложение водителя)

```bash
cd DriverApp
npm install --legacy-peer-deps
cd android
.\gradlew assembleDebug
```

APK файл: `DriverApp/android/app/build/outputs/apk/debug/app-debug.apk`

### PassengerApp (Приложение пассажира)

```bash
cd PassengerApp
npm install --legacy-peer-deps
cd android
.\gradlew assembleDebug
```

APK файл: `PassengerApp/android/app/build/outputs/apk/debug/app-debug.apk`

### Установка на телефон

**Вариант 1: Через USB**

```bash
adb install DriverApp/android/app/build/outputs/apk/debug/app-debug.apk
adb install PassengerApp/android/app/build/outputs/apk/debug/app-debug.apk
```

**Вариант 2: Скопировать APK на телефон**

1. Скопируйте APK файлы на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Установите приложение

---

## 🧪 Тестирование

### 1. Проверка сервера

```bash
# Проверка доступности
curl https://marshrutka.fun/api/v1/routes

# Должен вернуть JSON (пустой массив или данные)
```

### 2. Проверка SSL

```bash
# Проверка сертификата
openssl s_client -connect marshrutka.fun:443 -servername marshrutka.fun
```

### 3. Тестирование приложений

1. **Откройте DriverApp** на телефоне
2. Попробуйте войти (если есть тестовый аккаунт)
3. Проверьте что приложение не падает
4. Повторите для PassengerApp

### 4. Проверка логов

```bash
# Логи приложения
pm2 logs marshrutka-backend

# Логи Nginx
sudo tail -f /var/log/nginx/marshrutka_access.log
sudo tail -f /var/log/nginx/marshrutka_error.log
```

---

## 🔧 Решение проблем

### Приложение падает при запуске

**Причина**: Сервер недоступен или неправильный URL

**Решение**:
1. Проверьте что сервер запущен: `pm2 status`
2. Проверьте URL в `src/config/api.config.ts`
3. Проверьте логи: `pm2 logs marshrutka-backend`

### 502 Bad Gateway

**Причина**: Node.js приложение не запущено

**Решение**:
```bash
pm2 restart marshrutka-backend
pm2 logs marshrutka-backend
```

### SSL сертификат не работает

**Причина**: DNS не настроен или certbot не смог получить сертификат

**Решение**:
```bash
# Проверьте DNS
nslookup marshrutka.fun

# Попробуйте получить сертификат снова
sudo certbot --nginx -d marshrutka.fun -d www.marshrutka.fun
```

### База данных не подключается

**Причина**: Неправильные credentials или PostgreSQL не запущен

**Решение**:
```bash
# Проверьте статус PostgreSQL
sudo systemctl status postgresql

# Проверьте подключение
sudo -u postgres psql -U marshrutka_user -d marshrutka_db

# Проверьте .env файл
cat /var/www/marshrutka/.env
```

---

## 📊 Мониторинг

### Просмотр статуса

```bash
# Статус всех сервисов
pm2 status
sudo systemctl status nginx
sudo systemctl status postgresql
```

### Мониторинг ресурсов

```bash
# Использование CPU и памяти
pm2 monit

# Системные ресурсы
htop
```

### Логи в реальном времени

```bash
# Логи приложения
pm2 logs marshrutka-backend --lines 100

# Логи Nginx
sudo tail -f /var/log/nginx/marshrutka_access.log
```

---

## 🔄 Обновление

### Обновление backend

```bash
cd /var/www/marshrutka/server
git pull
npm install
npm run build
npm run migrate
pm2 restart marshrutka-backend
```

### Обновление мобильных приложений

1. Внесите изменения в код
2. Пересоберите APK
3. Распространите новую версию пользователям

---

## 🔐 Безопасность

### Рекомендации

1. ✅ Используйте сильные пароли для БД
2. ✅ Регулярно обновляйте систему: `sudo apt update && sudo apt upgrade`
3. ✅ Настройте firewall: `sudo ufw enable`
4. ✅ Используйте SSH ключи вместо паролей
5. ✅ Делайте регулярные бэкапы БД

### Резервное копирование

```bash
# Создание бэкапа
sudo -u postgres pg_dump marshrutka_db > backup_$(date +%Y%m%d).sql

# Восстановление
sudo -u postgres psql marshrutka_db < backup_20231114.sql
```

---

## 📞 Поддержка

### Полезные команды

```bash
# Перезапуск всех сервисов
pm2 restart all
sudo systemctl restart nginx
sudo systemctl restart postgresql

# Просмотр всех логов
pm2 logs
sudo journalctl -xe

# Проверка портов
sudo netstat -tulpn | grep LISTEN
```

### Документация

- [Подробная инструкция по развертыванию](server/DEPLOYMENT.md)
- [Конфигурация Nginx](server/nginx.conf)
- [Переменные окружения](server/.env.production)

---

## ✅ Чеклист развертывания

- [ ] DNS настроен и работает
- [ ] Сервер настроен и доступен
- [ ] Backend развернут и запущен
- [ ] SSL сертификат получен
- [ ] База данных создана и мигрирована
- [ ] Переменные окружения настроены
- [ ] API доступен по https://marshrutka.fun/api/v1
- [ ] Мобильные приложения собраны
- [ ] Приложения протестированы на телефоне
- [ ] Логи проверены, ошибок нет
- [ ] Настроено резервное копирование

---

**🎉 Поздравляем! Система Marshrutka развернута и готова к работе!**
