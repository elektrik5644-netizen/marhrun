# 🌐 Развертывание на Vercel через веб-интерфейс

## ⚠️ Важно

Если у вас проблемы с Vercel CLI (ошибка с кириллицей в имени пользователя Windows), используйте веб-интерфейс - это даже проще!

---

## 📋 Шаг 1: Подготовка Git репозитория

### Вариант A: GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub**
   - Зайдите на [github.com](https://github.com)
   - Нажмите "New repository"
   - Название: `marshrutka-app`
   - Сделайте приватным или публичным

2. **Загрузите код**

```bash
cd C:\Users\elekt\OneDrive\Desktop\marhrun

# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit"

# Добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/marshrutka-app.git

# Загрузите код
git push -u origin main
```

### Вариант B: GitLab или Bitbucket

Аналогично GitHub - создайте репозиторий и загрузите код.

---

## 🚀 Шаг 2: Развертывание на Vercel

### 1. Зайдите на Vercel

Откройте [vercel.com](https://vercel.com) и войдите через:
- GitHub (рекомендуется)
- GitLab
- Bitbucket
- Email

### 2. Создайте новый проект

1. Нажмите **"Add New..."** → **"Project"**
2. Выберите **"Import Git Repository"**
3. Найдите ваш репозиторий `marshrutka-app`
4. Нажмите **"Import"**

### 3. Настройте проект

На странице настройки проекта:

#### Framework Preset
```
Other
```

#### Root Directory
```
server
```
(Нажмите "Edit" и выберите папку `server`)

#### Build and Output Settings

```
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

#### Node.js Version
```
18.x (или 20.x)
```

### 4. Добавьте переменные окружения

Нажмите **"Environment Variables"** и добавьте:

```env
NODE_ENV=production
```

**Для базы данных** (выберите один из вариантов):

**Вариант A: Vercel Postgres** (проще всего)
- Пока пропустите, настроим после развертывания

**Вариант B: Внешняя БД** (Supabase, Neon, Railway)
```env
DB_HOST=ваш-хост.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=ваш-пароль
DB_NAME=postgres
```

**JWT и другие секреты:**
```env
JWT_SECRET=сгенерируйте_случайную_строку_32_символа
```

Для генерации JWT_SECRET используйте:
```bash
# В PowerShell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

### 5. Разверните проект

Нажмите **"Deploy"**

Vercel начнет:
- ✅ Клонировать репозиторий
- ✅ Устанавливать зависимости
- ✅ Собирать TypeScript
- ✅ Разворачивать приложение

Это займет 2-3 минуты.

---

## 🗄️ Шаг 3: Настройка базы данных

### Вариант A: Vercel Postgres (рекомендуется)

1. В вашем проекте на Vercel перейдите в **"Storage"**
2. Нажмите **"Create Database"**
3. Выберите **"Postgres"**
4. Выберите регион: **Europe (Frankfurt)** - ближе к России
5. Нажмите **"Create"**

Vercel автоматически:
- ✅ Создаст базу данных
- ✅ Добавит переменные окружения
- ✅ Перезапустит приложение

### Вариант B: Supabase (бесплатно)

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте аккаунт
3. Нажмите **"New project"**
4. Заполните:
   - Name: `marshrutka`
   - Database Password: (сохраните!)
   - Region: Europe (Frankfurt)
5. Нажмите **"Create new project"**

После создания:
1. Перейдите в **Settings → Database**
2. Скопируйте **Connection string** (URI)
3. Вернитесь в Vercel
4. **Settings → Environment Variables**
5. Добавьте переменные из connection string

### Запуск миграций

После настройки БД нужно запустить миграции:

**Локально:**
```bash
cd server

# Установите переменные окружения
$env:DATABASE_URL="postgresql://user:pass@host:5432/db"

# Запустите миграции
npm run migrate
```

**Или через Vercel CLI** (если работает):
```bash
vercel env pull .env.local
npm run migrate
```

---

## 🔗 Шаг 4: Подключение домена marshrutka.fun

### 1. В Vercel

1. Откройте ваш проект
2. Перейдите в **Settings → Domains**
3. Нажмите **"Add"**
4. Введите `marshrutka.fun`
5. Нажмите **"Add"**

### 2. Настройка DNS

Vercel покажет вам инструкции. Обычно это:

**Вариант A: A Record (проще)**

В панели управления вашего регистратора домена добавьте:

```
Тип    Имя    Значение           TTL
A      @      76.76.21.21       3600
A      www    76.76.21.21       3600
```

**Вариант B: CNAME (альтернатива)**

```
Тип     Имя    Значение                      TTL
CNAME   @      cname.vercel-dns.com         3600
CNAME   www    cname.vercel-dns.com         3600
```

**Вариант C: Vercel Nameservers (лучше всего)**

Измените nameservers у регистратора на:
```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

### 3. Проверка

DNS может обновляться до 48 часов, но обычно это занимает 10-30 минут.

Проверьте:
```bash
# Проверка DNS
nslookup marshrutka.fun

# Проверка доступности
curl https://marshrutka.fun
```

---

## ✅ Шаг 5: Проверка работы

### 1. Проверьте deployment

В Vercel Dashboard:
1. Откройте проект
2. Перейдите в **Deployments**
3. Последний deployment должен быть **"Ready"**
4. Нажмите на него
5. Нажмите **"Visit"**

### 2. Проверьте API

```bash
# Health check
curl https://ваш-проект.vercel.app

# Или с доменом
curl https://marshrutka.fun

# Должен вернуть:
# {"status":"ok","message":"Marshrutka API is running","version":"1.0.0"}
```

### 3. Проверьте маршруты

```bash
curl https://marshrutka.fun/api/v1/routes

# Должен вернуть [] или список маршрутов
```

### 4. Просмотрите логи

В Vercel Dashboard:
1. **Deployments** → Последний deployment
2. **View Function Logs**
3. Проверьте на ошибки

---

## 📱 Шаг 6: Обновление мобильных приложений

Приложения уже настроены на `https://marshrutka.fun/api/v1`

Просто пересоберите APK:

```bash
# DriverApp
cd C:\Users\elekt\OneDrive\Desktop\marhrun\DriverApp\android
.\gradlew assembleDebug

# PassengerApp
cd C:\Users\elekt\OneDrive\Desktop\marhrun\PassengerApp\android
.\gradlew assembleDebug
```

APK файлы:
- `DriverApp/android/app/build/outputs/apk/debug/app-debug.apk`
- `PassengerApp/android/app/build/outputs/apk/debug/app-debug.apk`

---

## 🔄 Обновление приложения

### Автоматическое развертывание

После настройки, каждый push в Git автоматически разворачивается:

```bash
# Внесите изменения в код
git add .
git commit -m "Update API"
git push origin main
```

Vercel автоматически:
- Соберет новую версию
- Развернет на production
- Обновит домен

### Просмотр прогресса

В Vercel Dashboard → Deployments вы увидите:
- 🟡 Building - идет сборка
- 🟢 Ready - готово
- 🔴 Error - ошибка

---

## 🚨 Решение проблем

### Deployment Failed

**Причина**: Ошибка в коде или зависимостях

**Решение**:
1. Откройте deployment в Vercel
2. Посмотрите логи сборки
3. Исправьте ошибку
4. Сделайте новый commit и push

### API возвращает 500

**Причина**: Ошибка в коде или неправильные переменные окружения

**Решение**:
1. Проверьте **Function Logs** в Vercel
2. Проверьте **Environment Variables**
3. Убедитесь что база данных доступна

### База данных не подключается

**Причина**: Неправильные credentials или firewall

**Решение**:
1. Проверьте переменные окружения
2. Для Supabase: убедитесь что включен "Connection pooling"
3. Проверьте что IP Vercel разрешен (обычно разрешены все)

### Домен не работает

**Причина**: DNS не настроен или не распространился

**Решение**:
1. Проверьте DNS: `nslookup marshrutka.fun`
2. Подождите до 48 часов
3. Проверьте настройки в Vercel → Domains
4. Убедитесь что SSL сертификат выдан (обычно автоматически)

---

## 📊 Мониторинг

### Analytics

В Vercel Dashboard → Analytics:
- Количество запросов
- Время ответа
- География пользователей
- Ошибки

### Логи

**Real-time логи:**
1. Deployments → Latest
2. View Function Logs
3. Фильтруйте по типу (Info, Warning, Error)

**Или через CLI** (если работает):
```bash
vercel logs
```

---

## 💰 Стоимость

### Vercel Hobby (Free)
- ✅ 100 GB bandwidth/месяц
- ✅ Serverless Functions: 100 GB-Hours
- ✅ Unlimited deployments
- ✅ SSL сертификаты
- ✅ Подходит для тестирования и небольших проектов

### Vercel Pro ($20/месяц)
- ✅ 1 TB bandwidth
- ✅ Serverless Functions: 1000 GB-Hours
- ✅ Приоритетная поддержка
- ✅ Подходит для production

### База данных

- **Vercel Postgres**: от $0.25/месяц
- **Supabase Free**: до 500 MB, 2 GB transfer
- **Neon Free**: до 3 GB

---

## ✅ Чеклист

- [ ] Код загружен в Git (GitHub/GitLab/Bitbucket)
- [ ] Проект создан на Vercel
- [ ] Root Directory установлен на `server`
- [ ] Environment Variables добавлены
- [ ] Deployment успешен (статус Ready)
- [ ] База данных создана и настроена
- [ ] Миграции запущены
- [ ] API доступен и отвечает
- [ ] Домен marshrutka.fun добавлен
- [ ] DNS настроен
- [ ] SSL сертификат активен
- [ ] Мобильные приложения пересобраны
- [ ] Приложения протестированы на телефоне

---

## 📞 Поддержка

### Документация Vercel
- [Getting Started](https://vercel.com/docs)
- [Deploying Node.js](https://vercel.com/docs/frameworks/nodejs)
- [Custom Domains](https://vercel.com/docs/concepts/projects/custom-domains)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

### Сообщество
- [Vercel Discord](https://vercel.com/discord)
- [GitHub Discussions](https://github.com/vercel/vercel/discussions)

---

**🎉 Готово! Ваш backend работает на Vercel с доменом marshrutka.fun!**

**Следующие шаги:**
1. Протестируйте API
2. Установите мобильные приложения на телефон
3. Создайте тестовые данные (маршруты, рейсы)
4. Начните использовать систему!
