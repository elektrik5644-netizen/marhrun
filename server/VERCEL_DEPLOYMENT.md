# 🚀 Развертывание на Vercel с доменом marshrutka.fun

## 📋 Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Домен marshrutka.fun
3. База данных PostgreSQL (можно использовать Vercel Postgres или внешний сервис)

---

## 🎯 Шаг 1: Подготовка проекта

### 1.1 Установите Vercel CLI (опционально)

```bash
npm install -g vercel
```

### 1.2 Проверьте файлы

Убедитесь что в папке `server/` есть:
- ✅ `vercel.json` - конфигурация Vercel
- ✅ `src/app.ts` - основное приложение
- ✅ `package.json` - зависимости

---

## 🌐 Шаг 2: Развертывание на Vercel

### Вариант A: Через веб-интерфейс (рекомендуется)

1. **Зайдите на [vercel.com](https://vercel.com)**

2. **Нажмите "Add New Project"**

3. **Импортируйте Git репозиторий**
   - Подключите GitHub/GitLab/Bitbucket
   - Выберите ваш репозиторий

4. **Настройте проект**
   ```
   Framework Preset: Other
   Root Directory: server
   Build Command: npm run build
   Output Directory: dist
   Install Command: npm install
   ```

5. **Добавьте переменные окружения**
   
   Нажмите "Environment Variables" и добавьте:
   
   ```env
   NODE_ENV=production
   
   # База данных (используйте Vercel Postgres или внешний сервис)
   DB_HOST=your-db-host.com
   DB_PORT=5432
   DB_USER=your-db-user
   DB_PASSWORD=your-db-password
   DB_NAME=marshrutka_db
   
   # JWT секрет
   JWT_SECRET=ваш_случайный_секрет_минимум_32_символа
   
   # YooKassa
   YOOKASSA_SHOP_ID=your-shop-id
   YOOKASSA_SECRET_KEY=your-secret-key
   
   # Яндекс Карты
   YANDEX_MAPS_API_KEY=your-api-key
   ```

6. **Нажмите "Deploy"**

   Vercel автоматически:
   - Установит зависимости
   - Соберет TypeScript
   - Развернет приложение
   - Даст вам URL типа `your-project.vercel.app`

### Вариант B: Через CLI

```bash
cd server
vercel

# Следуйте инструкциям:
# - Set up and deploy? Yes
# - Which scope? Выберите ваш аккаунт
# - Link to existing project? No
# - What's your project's name? marshrutka-backend
# - In which directory is your code located? ./
```

---

## 🔗 Шаг 3: Подключение домена marshrutka.fun

### 3.1 В панели Vercel

1. Откройте ваш проект
2. Перейдите в **Settings → Domains**
3. Нажмите **Add Domain**
4. Введите `marshrutka.fun`
5. Нажмите **Add**

### 3.2 Настройка DNS

Vercel покажет вам DNS записи. Добавьте их в панели управления вашего регистратора:

**Для корневого домена (marshrutka.fun):**

```
Тип    Имя    Значение                    TTL
A      @      76.76.21.21                3600
```

**Для www поддомена:**

```
Тип     Имя    Значение                   TTL
CNAME   www    cname.vercel-dns.com      3600
```

**Или используйте Vercel Nameservers (рекомендуется):**

В настройках домена у регистратора измените nameservers на:

```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

### 3.3 Проверка

После добавления DNS записей (может занять до 48 часов):

```bash
# Проверка DNS
nslookup marshrutka.fun

# Проверка доступности
curl https://marshrutka.fun
curl https://marshrutka.fun/api/v1/routes
```

---

## 🗄️ Шаг 4: Настройка базы данных

### Вариант A: Vercel Postgres (рекомендуется)

1. В проекте на Vercel перейдите в **Storage**
2. Нажмите **Create Database**
3. Выберите **Postgres**
4. Выберите регион (ближайший к вашим пользователям)
5. Нажмите **Create**

Vercel автоматически добавит переменные окружения:
- `POSTGRES_URL`
- `POSTGRES_PRISMA_URL`
- `POSTGRES_URL_NON_POOLING`

Обновите `knexfile.js` для использования этих переменных.

### Вариант B: Внешняя база данных

Используйте любой PostgreSQL хостинг:
- [Supabase](https://supabase.com) - бесплатный tier
- [Neon](https://neon.tech) - serverless PostgreSQL
- [Railway](https://railway.app)
- [ElephantSQL](https://www.elephantsql.com)

Добавьте credentials в Environment Variables на Vercel.

### Запуск миграций

```bash
# Локально с подключением к production БД
DATABASE_URL="postgresql://user:pass@host:5432/db" npm run migrate

# Или через Vercel CLI
vercel env pull .env.local
npm run migrate
```

---

## 🔧 Шаг 5: Обновление мобильных приложений

Обновите API URL в приложениях:

### DriverApp

```typescript
// DriverApp/src/config/api.config.ts
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

### PassengerApp

```typescript
// PassengerApp/src/config/api.config.ts
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

### Пересборка приложений

```bash
# DriverApp
cd DriverApp/android
.\gradlew assembleDebug

# PassengerApp
cd PassengerApp/android
.\gradlew assembleDebug
```

---

## ✅ Шаг 6: Тестирование

### 6.1 Проверка API

```bash
# Health check
curl https://marshrutka.fun

# Должен вернуть:
# {"status":"ok","message":"Marshrutka API is running","version":"1.0.0"}

# Проверка маршрутов
curl https://marshrutka.fun/api/v1/routes
```

### 6.2 Проверка в приложениях

1. Установите обновленные APK на телефон
2. Откройте приложение
3. Проверьте что нет ошибок подключения
4. Попробуйте войти/зарегистрироваться

### 6.3 Просмотр логов

В Vercel Dashboard:
1. Откройте проект
2. Перейдите в **Deployments**
3. Выберите последний deployment
4. Нажмите **View Function Logs**

---

## 🔄 Обновление приложения

### Автоматическое развертывание

Vercel автоматически разворачивает при каждом push в Git:

```bash
git add .
git commit -m "Update API"
git push origin main
```

Vercel автоматически:
- Соберет новую версию
- Запустит тесты (если настроены)
- Развернет на production
- Обновит домен

### Ручное развертывание

```bash
cd server
vercel --prod
```

---

## 📊 Мониторинг

### Vercel Analytics

1. В проекте перейдите в **Analytics**
2. Просмотрите:
   - Количество запросов
   - Время ответа
   - Ошибки
   - География пользователей

### Логи

```bash
# Просмотр логов через CLI
vercel logs

# Или в веб-интерфейсе
# Deployments → Latest → View Function Logs
```

---

## 🚨 Решение проблем

### Ошибка 500 Internal Server Error

**Причина**: Ошибка в коде или неправильные переменные окружения

**Решение**:
1. Проверьте логи в Vercel Dashboard
2. Проверьте Environment Variables
3. Убедитесь что база данных доступна

### База данных не подключается

**Причина**: Неправильные credentials или firewall

**Решение**:
1. Проверьте переменные окружения
2. Убедитесь что IP Vercel разрешен в firewall БД
3. Для Vercel Postgres проверьте что используете правильный URL

### Домен не работает

**Причина**: DNS не настроен или не распространился

**Решение**:
1. Проверьте DNS: `nslookup marshrutka.fun`
2. Подождите до 48 часов для распространения DNS
3. Проверьте настройки в Vercel Dashboard → Domains

### CORS ошибки

**Причина**: Домен не добавлен в CORS whitelist

**Решение**:
Обновите `src/app.ts`:

```typescript
const corsOptions = {
  origin: [
    'https://marshrutka.fun',
    'https://www.marshrutka.fun',
    'https://*.vercel.app'
  ],
  credentials: true,
  optionsSuccessStatus: 200
};
```

---

## 💰 Стоимость

### Vercel Pricing

- **Hobby (Free)**:
  - 100 GB bandwidth/месяц
  - Serverless Functions: 100 GB-Hours
  - Подходит для тестирования

- **Pro ($20/месяц)**:
  - 1 TB bandwidth
  - Serverless Functions: 1000 GB-Hours
  - Подходит для production

### База данных

- **Vercel Postgres**: от $0.25/месяц
- **Supabase**: бесплатный tier до 500 MB
- **Neon**: бесплатный tier до 3 GB

---

## 🔐 Безопасность

### Рекомендации

1. ✅ Используйте сильные пароли для БД
2. ✅ Храните секреты в Environment Variables
3. ✅ Включите Vercel Authentication для защиты endpoints
4. ✅ Настройте rate limiting
5. ✅ Регулярно обновляйте зависимости

### Environment Variables

Никогда не коммитьте `.env` файлы в Git!

Используйте:
```bash
# Добавить переменную
vercel env add JWT_SECRET

# Скачать переменные локально
vercel env pull .env.local
```

---

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
- [Custom Domains](https://vercel.com/docs/concepts/projects/custom-domains)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

## ✅ Чеклист развертывания

- [ ] Проект создан на Vercel
- [ ] Переменные окружения настроены
- [ ] База данных создана и доступна
- [ ] Миграции запущены
- [ ] Домен marshrutka.fun добавлен
- [ ] DNS настроен
- [ ] SSL сертификат активен
- [ ] API доступен по https://marshrutka.fun/api/v1
- [ ] Мобильные приложения обновлены
- [ ] Приложения протестированы
- [ ] Логи проверены, ошибок нет

---

**🎉 Готово! Ваш backend работает на Vercel с доменом marshrutka.fun!**
