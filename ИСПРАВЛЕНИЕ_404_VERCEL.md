# 🔧 Исправление ошибки 404 на Vercel

## ❌ Проблема

Сервер возвращает `/404.html` вместо правильных ответов API.

## ✅ Решения

### 1. Проверьте конфигурацию vercel.json

Убедитесь что `vercel.json` правильно настроен:

```json
{
  "version": 2,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/v1/(.*)",
      "dest": "src/app.ts"
    },
    {
      "src": "/socket.io/(.*)",
      "dest": "src/app.ts"
    },
    {
      "src": "/(.*)",
      "dest": "src/app.ts"
    }
  ]
}
```

### 2. Проверьте Root Directory в Vercel

В настройках проекта на Vercel:
- **Settings → General → Root Directory** должно быть: `server`

### 3. Проверьте экспорт app.ts

Файл `server/src/app.ts` должен экспортировать Express app:

```typescript
export default app;
```

### 4. Пересоберите проект на Vercel

1. Откройте проект на Vercel
2. Перейдите в **Deployments**
3. Нажмите **Redeploy** на последнем deployment
4. Или сделайте новый commit и push:

```bash
git add .
git commit -m "Исправлена конфигурация Vercel"
git push origin main
```

### 5. Проверьте логи

В Vercel Dashboard:
- **Deployments → Latest → View Function Logs**
- Проверьте что нет ошибок при запуске

---

## 🔍 Диагностика

### Проверка 1: Health Check

```bash
curl https://marshrutka.fun
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "Marshrutka API is running",
  "version": "1.0.0"
}
```

### Проверка 2: API Endpoint

```bash
curl https://marshrutka.fun/api/v1/routes
```

Должен вернуть список маршрутов или ошибку авторизации (не 404).

---

## 🚨 Частые причины 404

### 1. Неправильный Root Directory

**Решение:** Установите Root Directory = `server` в настройках Vercel

### 2. Неправильные пути в vercel.json

**Решение:** Убедитесь что `dest` указывает на правильный файл без лишних слэшей

### 3. Файл не найден

**Решение:** Проверьте что файл `src/app.ts` существует в папке `server`

### 4. Ошибка компиляции TypeScript

**Решение:** Проверьте логи сборки в Vercel Dashboard

---

## ✅ Чеклист исправления

- [ ] Root Directory установлен на `server`
- [ ] vercel.json правильно настроен
- [ ] app.ts экспортирует Express app
- [ ] Проект пересобран на Vercel
- [ ] Логи проверены на ошибки
- [ ] Health check работает: `curl https://marshrutka.fun`
- [ ] API endpoints работают: `curl https://marshrutka.fun/api/v1/routes`

---

## 📝 После исправления

После того как исправите конфигурацию:

1. **Сделайте commit и push:**
   ```bash
   git add .
   git commit -m "Исправлена конфигурация Vercel"
   git push origin main
   ```

2. **Vercel автоматически пересоберет проект**

3. **Проверьте что все работает:**
   ```bash
   curl https://marshrutka.fun
   curl https://marshrutka.fun/api/v1/routes
   ```

---

**💡 Главное:** Убедитесь что Root Directory установлен на `server` в настройках Vercel!

