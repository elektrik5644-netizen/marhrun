# 🔧 Исправление ошибки ERR_CONNECTION_REFUSED

## ❌ Проблема

Ошибка `ERR_CONNECTION_REFUSED` означает, что домен `marshrutka.fun` указывает не на Vercel.

**Текущий IP:** 194.58.112.174 (это НЕ IP Vercel)

**Нужный IP для Vercel:** 76.76.21.21

---

## ✅ Решение

### Шаг 1: Проверьте проект на Vercel

1. Откройте: [https://vercel.com/sergeis-projects-78aa3a6d](https://vercel.com/sergeis-projects-78aa3a6d)

2. Проверьте что проект развернут:
   - Перейдите в **Deployments**
   - Должен быть хотя бы один успешный deployment со статусом ✅ **Ready**

3. Если проекта нет или он не развернут:
   - См. инструкцию: `DEPLOY_MARSHRUTKA_FUN.md`
   - Или `ПОДКЛЮЧЕНИЕ_ДОМЕНА.md`

---

### Шаг 2: Добавьте домен в Vercel (если еще не добавлен)

1. В проекте на Vercel перейдите в **Settings → Domains**

2. Нажмите **"Add Domain"**

3. Введите: `marshrutka.fun`

4. Нажмите **"Add"**

5. Vercel покажет DNS записи, которые нужно добавить

---

### Шаг 3: Исправьте DNS записи у регистратора домена

Откройте панель управления вашего регистратора домена (где вы купили marshrutka.fun).

#### Вариант A: Использование A записи (для корневого домена)

**Удалите старую A запись** (если есть) и добавьте новую:

```
Тип: A
Имя: @ (или оставьте пустым для корневого домена)
Значение: 76.76.21.21
TTL: 3600 (или Auto)
```

#### Вариант B: Использование Vercel Nameservers (РЕКОМЕНДУЕТСЯ)

В настройках домена у регистратора измените **nameservers** на:

```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

Это позволит Vercel полностью управлять DNS записями автоматически.

**Как изменить nameservers:**
1. В панели регистратора найдите раздел "DNS" или "Nameservers"
2. Выберите "Custom Nameservers" или "Изменить nameservers"
3. Введите:
   - `ns1.vercel-dns.com`
   - `ns2.vercel-dns.com`
4. Сохраните изменения

---

### Шаг 4: Проверьте настройки проекта в Vercel

Убедитесь что:

1. **Root Directory** установлен на `server`:
   - **Settings → General → Root Directory** = `server`

2. **Build Settings** правильные:
   - **Framework Preset:** `Other`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`

3. **Переменные окружения** добавлены:
   - **Settings → Environment Variables**
   - Должны быть: `NODE_ENV=production`, `JWT_SECRET=...`, и т.д.

---

### Шаг 5: Ожидание распространения DNS

После изменения DNS записей:

1. **Подождите 5-60 минут** (иногда до 48 часов)

2. **Проверьте DNS:**
   ```powershell
   nslookup marshrutka.fun
   ```
   
   Должен вернуть IP: **76.76.21.21** (или другой IP Vercel)

3. **Проверьте статус в Vercel:**
   - **Settings → Domains → marshrutka.fun**
   - Статус должен измениться с ⏳ **Pending** на ✅ **Valid**

---

## 🔍 Проверка что все работает

### 1. Проверка DNS (через 5-60 минут после изменения)

```powershell
nslookup marshrutka.fun
```

Должен вернуть IP начинающийся с `76.76.x.x` или другой IP Vercel (НЕ 194.58.112.174)

### 2. Проверка в браузере

Откройте: **https://marshrutka.fun**

Должен вернуться JSON:
```json
{
  "status": "ok",
  "message": "Marshrutka API is running",
  "version": "1.0.0"
}
```

### 3. Проверка через PowerShell

```powershell
Invoke-WebRequest -Uri "https://marshrutka.fun" -UseBasicParsing
```

---

## 🚨 Если проблема не решается

### Проблема: DNS все еще показывает старый IP

**Решение:**
1. Убедитесь что вы сохранили изменения DNS у регистратора
2. Очистите DNS кэш:
   ```powershell
   ipconfig /flushdns
   ```
3. Подождите еще 10-30 минут

### Проблема: Домен не добавляется в Vercel

**Решение:**
1. Убедитесь что проект развернут (есть успешный deployment)
2. Проверьте что Root Directory установлен на `server`
3. Убедитесь что домен не используется в другом проекте Vercel

### Проблема: После изменения DNS все еще ERR_CONNECTION_REFUSED

**Решение:**
1. Проверьте что проект развернут на Vercel (Deployments → Ready)
2. Проверьте логи: Deployments → Latest → View Function Logs
3. Убедитесь что нет ошибок в коде

---

## 📋 Чеклист исправления

- [ ] Проект развернут на Vercel (Deployments → Ready)
- [ ] Домен `marshrutka.fun` добавлен в Vercel (Settings → Domains)
- [ ] Root Directory установлен на `server`
- [ ] DNS записи изменены у регистратора (A запись → 76.76.21.21 или nameservers → Vercel)
- [ ] Подождали 5-60 минут после изменения DNS
- [ ] DNS проверен: `nslookup marshrutka.fun` показывает IP Vercel
- [ ] Домен открывается в браузере без ошибки ERR_CONNECTION_REFUSED
- [ ] SSL сертификат активен (зеленый замочек)

---

## 💡 Важно

**Текущая проблема:** Домен указывает на IP 194.58.112.174, который не является IP Vercel.

**Решение:** Измените DNS записи у регистратора домена на IP Vercel (76.76.21.21) или используйте Vercel Nameservers.

После изменения DNS подождите 5-60 минут и проверьте снова.

---

**🎯 Главное:** Убедитесь что DNS записи у регистратора указывают на Vercel, а не на другой сервер!

