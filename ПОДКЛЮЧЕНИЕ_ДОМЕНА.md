# 🔗 Подключение marshrutka.fun к проекту на Vercel

## 📍 Ваш проект на Vercel

Проект: [https://vercel.com/sergeis-projects-78aa3a6d/~/deployments](https://vercel.com/sergeis-projects-78aa3a6d/~/deployments)

---

## 🚀 Быстрая инструкция (3 шага)

### Шаг 1: Добавьте домен в Vercel

1. Откройте ваш проект на Vercel: [https://vercel.com/sergeis-projects-78aa3a6d](https://vercel.com/sergeis-projects-78aa3a6d)

2. Перейдите в **Settings** → **Domains**

3. Нажмите **"Add Domain"**

4. Введите: `marshrutka.fun`

5. Нажмите **"Add"**

6. Vercel покажет DNS записи, которые нужно добавить

---

### Шаг 2: Настройте DNS у регистратора домена

Откройте панель управления вашего регистратора домена (где вы купили marshrutka.fun) и добавьте DNS записи:

#### Вариант A: A запись (для корневого домена)

```
Тип: A
Имя: @ (или оставьте пустым для корневого домена)
Значение: 76.76.21.21
TTL: 3600 (или Auto)
```

#### Вариант B: CNAME (для www поддомена, опционально)

```
Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
TTL: 3600
```

#### Вариант C: Vercel Nameservers (рекомендуется)

В настройках домена у регистратора измените nameservers на:

```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

Это позволит Vercel полностью управлять DNS записями автоматически.

---

### Шаг 3: Ожидание и проверка

1. **Подождите 5-60 минут** (иногда до 48 часов) для распространения DNS

2. **Проверьте статус в Vercel:**
   - Откройте **Settings → Domains**
   - Статус должен измениться с ⏳ **Pending** на ✅ **Valid**

3. **Проверьте работу домена:**
   ```bash
   # Проверка DNS
   nslookup marshrutka.fun
   
   # Проверка API
   curl https://marshrutka.fun
   ```

   Должен вернуться ответ:
   ```json
   {
     "status": "ok",
     "message": "Marshrutka API is running",
     "version": "1.0.0"
   }
   ```

4. **Откройте в браузере:**
   - `https://marshrutka.fun` - должен работать с SSL сертификатом (зеленый замочек)

---

## ⚙️ Проверка настроек проекта

Убедитесь что в проекте правильно настроено:

### 1. Root Directory

В **Settings → General** должно быть:
```
Root Directory: server
```

### 2. Build Settings

```
Framework Preset: Other
Build Command: npm run build
Output Directory: dist
```

### 3. Переменные окружения

В **Settings → Environment Variables** должны быть:

```env
NODE_ENV=production
JWT_SECRET=ваш_секрет_32_символа
DATABASE_URL=postgresql://...
```

---

## 🔄 Автоматическое развертывание

После подключения домена, каждый `git push` будет автоматически обновлять marshrutka.fun:

```bash
git add .
git commit -m "Обновление"
git push origin main
```

Vercel автоматически:
- Соберет проект
- Развернет на production
- Обновит домен marshrutka.fun

---

## 🚨 Решение проблем

### Домен показывает "Pending" долгое время

1. Проверьте DNS записи: `nslookup marshrutka.fun`
2. Убедитесь что записи добавлены правильно
3. Подождите до 48 часов (обычно 5-60 минут)

### Домен не работает после настройки

1. Проверьте что Root Directory установлен на `server`
2. Проверьте логи: **Deployments → Latest → View Function Logs**
3. Убедитесь что проект успешно развернут

### SSL сертификат не работает

Vercel автоматически выдает SSL сертификаты. Если не работает:
1. Подождите 5-10 минут после добавления домена
2. Проверьте что DNS записи настроены правильно
3. В Vercel Dashboard → Domains должен быть статус "Valid"

---

## 📱 Обновление мобильных приложений

После того как домен заработает, обновите API URL:

### DriverApp
`DriverApp/src/config/api.config.ts`:
```typescript
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

### PassengerApp
`PassengerApp/src/config/api.config.ts`:
```typescript
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

---

## ✅ Чеклист

- [ ] Домен marshrutka.fun добавлен в Vercel → Settings → Domains
- [ ] DNS записи настроены у регистратора домена
- [ ] Статус домена в Vercel: ✅ Valid
- [ ] SSL сертификат активен (зеленый замочек в браузере)
- [ ] API доступен: `curl https://marshrutka.fun` возвращает ответ
- [ ] Root Directory установлен на `server`
- [ ] Переменные окружения настроены
- [ ] Мобильные приложения обновлены с новым API URL

---

**🎉 Готово! Домен marshrutka.fun подключен к вашему проекту на Vercel!**

