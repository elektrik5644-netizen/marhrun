# ✅ Проверка мобильного приложения для пассажиров (PassengerApp)

## 📋 Результаты проверки

### ✅ Что работает правильно:

1. **Конфигурация API** (`src/config/api.config.ts`)
   - ✅ BASE_URL: `https://marshrutka.fun/api/v1` - правильно настроен
   - ✅ SOCKET_URL: `https://marshrutka.fun` - правильно настроен (хотя Socket.IO не используется)
   - ✅ TIMEOUT: 15000ms - разумное значение

2. **Структура приложения**
   - ✅ App.tsx - правильно настроен с AuthProvider и PaperProvider
   - ✅ Навигация (AppNavigator) - работает с авторизацией
   - ✅ Экраны: LoginScreen, RegisterScreen, MapScreen, TripBookingScreen, RideHistoryScreen, ProfileScreen
   - ✅ Сервисы: api.ts с authService, routeService, tripService, orderService

3. **UI компоненты**
   - ✅ Используется react-native-paper для Material Design
   - ✅ react-native-maps для отображения карты
   - ✅ Все экраны реализованы с правильной навигацией

4. **Авторизация**
   - ✅ AuthContext - правильно реализован
   - ✅ Токены сохраняются в AsyncStorage (userToken, userData)
   - ✅ Автоматическая проверка авторизации при запуске

---

## 🔧 Исправленные проблемы:

### 1. Android разрешения (ИСПРАВЛЕНО)

**Проблема:** В AndroidManifest.xml не было разрешений для геолокации, необходимых для карт.

**Исправление:**
- ✅ Добавлены разрешения: `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION`

### 2. Network Security Config (ДОБАВЛЕНО)

**Проблема:** Android может блокировать HTTPS соединения без правильной конфигурации.

**Исправление:**
- ✅ Создан файл `network_security_config.xml`
- ✅ Добавлен в AndroidManifest.xml
- ✅ Разрешены HTTPS соединения

---

## 🧪 Что нужно протестировать:

### 1. Сборка приложения

```bash
cd PassengerApp/android
.\gradlew assembleDebug
```

### 2. Установка на устройство

```bash
cd PassengerApp
npm run android
```

### 3. Функциональное тестирование

#### Тест 1: Регистрация
- [ ] Открыть приложение
- [ ] Нажать "Зарегистрироваться"
- [ ] Заполнить все поля (имя, фамилия, телефон, пароль)
- [ ] Проверить валидацию пароля (минимум 6 символов)
- [ ] Проверить что регистрация работает
- [ ] Проверить что после регистрации открывается карта

#### Тест 2: Авторизация
- [ ] Выйти из приложения (если зарегистрирован)
- [ ] Ввести телефон и пароль
- [ ] Проверить что вход работает
- [ ] Проверить что токен сохраняется

#### Тест 3: Карта маршрутов
- [ ] После входа должен открыться экран с картой
- [ ] Проверить что маршруты загружаются с сервера
- [ ] Проверить отображение маркеров на карте (зеленый - отправление, красный - прибытие)
- [ ] Проверить поиск маршрутов по городам
- [ ] Выбрать маршрут и проверить отображение информации

#### Тест 4: Бронирование рейса
- [ ] Выбрать маршрут на карте
- [ ] Нажать "Выбрать рейс"
- [ ] Выбрать дату
- [ ] Проверить загрузку доступных рейсов
- [ ] Выбрать рейс
- [ ] Указать количество мест (1-4)
- [ ] Выбрать способ оплаты (наличные/карта)
- [ ] Нажать "Забронировать"
- [ ] Проверить что заказ создается успешно

#### Тест 5: История поездок
- [ ] Открыть меню (FAB)
- [ ] Нажать "История поездок"
- [ ] Проверить отображение созданных заказов
- [ ] Проверить возможность отмены заказа

#### Тест 6: Профиль
- [ ] Открыть меню (FAB)
- [ ] Нажать "Профиль"
- [ ] Проверить отображение данных пользователя
- [ ] Проверить выход из аккаунта

---

## 🚨 Возможные проблемы и решения:

### Проблема: "Network request failed"

**Причины:**
1. Сервер не доступен (marshrutka.fun не работает)
2. Неправильный API URL
3. Проблемы с SSL сертификатом

**Решение:**
1. Проверьте что сервер работает: `curl https://marshrutka.fun`
2. Проверьте `api.config.ts` - должен быть правильный URL
3. Проверьте SSL сертификат на сервере

### Проблема: "401 Unauthorized"

**Причины:**
1. Токен не сохраняется
2. Токен истек
3. Неправильный формат токена

**Решение:**
1. Проверьте что токен сохраняется в AsyncStorage (userToken)
2. Попробуйте выйти и войти снова
3. Проверьте формат токена в заголовке Authorization

### Проблема: Карта не отображается

**Причины:**
1. Не установлен Google Play Services
2. Неправильный API ключ Google Maps
3. Проблемы с разрешениями геолокации

**Решение:**
1. Убедитесь что на устройстве установлен Google Play Services
2. Для production нужен API ключ Google Maps (добавить в AndroidManifest.xml)
3. Проверьте разрешения геолокации в настройках устройства

### Проблема: Приложение крашится при запуске

**Причины:**
1. Ошибки в коде
2. Проблемы с зависимостями
3. Проблемы с нативными модулями (react-native-maps, react-native-paper)

**Решение:**
1. Проверьте логи: `adb logcat | grep ReactNativeJS`
2. Переустановите зависимости: `npm install`
3. Очистите кэш: `npm start -- --reset-cache`
4. Для react-native-maps может потребоваться дополнительная настройка

---

## 📱 Настройки для production:

### 1. Обновите API URL (если нужно)

Файл: `PassengerApp/src/config/api.config.ts`

```typescript
export const API_CONFIG = {
  BASE_URL: 'https://marshrutka.fun/api/v1',
  TIMEOUT: 15000,
  SOCKET_URL: 'https://marshrutka.fun',
};
```

### 2. Добавьте Google Maps API ключ

Для работы карт в production нужен API ключ Google Maps:

1. Получите API ключ в [Google Cloud Console](https://console.cloud.google.com/)
2. Добавьте в `AndroidManifest.xml`:

```xml
<meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="YOUR_API_KEY_HERE"/>
```

### 3. Соберите release APK

```bash
cd PassengerApp/android
.\gradlew assembleRelease
```

APK будет в: `PassengerApp/android/app/build/outputs/apk/release/app-release.apk`

### 4. Подпишите APK (для Google Play)

Следуйте инструкциям Google Play Console для подписи приложения.

---

## ✅ Чеклист перед релизом:

- [ ] API URL правильный (marshrutka.fun)
- [ ] Все зависимости установлены
- [ ] Приложение собирается без ошибок
- [ ] Регистрация работает
- [ ] Авторизация работает
- [ ] Карта отображается и загружает маршруты
- [ ] Бронирование рейсов работает
- [ ] История поездок работает
- [ ] Профиль работает
- [ ] Нет ошибок в консоли
- [ ] Приложение не крашится
- [ ] Google Maps API ключ добавлен (для production)
- [ ] Release APK собран
- [ ] APK подписан (для Google Play)

---

## 📚 Полезные команды:

### Запуск приложения

```bash
cd PassengerApp
npm start
npm run android
```

### Просмотр логов

```bash
# Android
adb logcat | grep ReactNativeJS

# Или через Metro bundler
npm start
```

### Очистка и пересборка

```bash
cd PassengerApp/android
.\gradlew clean
.\gradlew assembleDebug
```

### Установка зависимостей

```bash
cd PassengerApp
npm install
cd android
.\gradlew clean
```

---

## 🔗 Связанные файлы:

- `PassengerApp/src/config/api.config.ts` - конфигурация API
- `PassengerApp/src/services/api.ts` - HTTP клиент и сервисы
- `PassengerApp/src/contexts/AuthContext.tsx` - контекст авторизации
- `PassengerApp/src/navigation/AppNavigator.js` - навигация
- `PassengerApp/src/screens/` - все экраны приложения
- `PassengerApp/android/app/src/main/AndroidManifest.xml` - манифест Android
- `PassengerApp/android/app/src/main/res/xml/network_security_config.xml` - конфигурация безопасности сети

---

## 📝 Особенности приложения:

1. **Material Design** - используется react-native-paper для единого стиля
2. **Карты** - react-native-maps для отображения маршрутов
3. **Навигация** - Stack Navigator с условной навигацией (авторизован/не авторизован)
4. **Бронирование** - полный цикл от выбора маршрута до создания заказа
5. **История** - просмотр и управление заказами

---

**🎉 Приложение готово к тестированию!**

