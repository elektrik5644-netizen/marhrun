# 🔧 Финальное исправление ошибки 404 на Vercel

## ✅ Что исправлено

1. **Создана правильная структура для Vercel:**
   - Создан файл `server/api/index.ts` - точка входа для Vercel serverless function
   - Обновлен `vercel.json` для использования `api/index.ts`
   - Обновлен `tsconfig.json` для включения папки `api/`

2. **Упрощена конфигурация маршрутов:**
   - Все запросы теперь идут через один handler: `/api/index.ts`
   - Express app обрабатывает все маршруты внутри себя

---

## 🚨 КРИТИЧЕСКИ ВАЖНО: Проверьте Root Directory!

### Шаг 1: Откройте проект на Vercel

👉 [https://vercel.com/sergeis-projects-78aa3a6d](https://vercel.com/sergeis-projects-78aa3a6d)

### Шаг 2: Проверьте Root Directory

1. Перейдите в **Settings → General**
2. Найдите поле **Root Directory**
3. **ДОЛЖНО БЫТЬ:** `server` ✅
4. Если пусто или другое значение:
   - Нажмите **Edit**
   - Введите: `server`
   - Нажмите **Save**

### Шаг 3: Пересоберите проект

После изменения Root Directory:

1. Перейдите в **Deployments**
2. Нажмите **Redeploy** на последнем deployment
3. Или подождите автоматической пересборки (после push на GitHub)

---

## 📋 Новая структура

```
server/
  ├── api/
  │   └── index.ts          ← Точка входа для Vercel
  ├── src/
  │   └── app.ts           ← Express приложение
  └── vercel.json          ← Конфигурация Vercel
```

---

## 🔍 Проверка после исправления

Подождите 2-3 минуты после пересборки, затем проверьте:

```bash
# Health check
curl https://marshrutka.fun

# Должен вернуть:
# {"status":"ok","message":"Marshrutka API is running","version":"1.0.0"}

# API endpoint
curl https://marshrutka.fun/api/v1/routes
```

---

## 🚨 Если все еще 404

### 1. Проверьте логи сборки

В **Deployments → Latest → Build Logs**:
- ✅ Нет ошибок компиляции TypeScript
- ✅ Файл `api/index.ts` найден
- ✅ Все зависимости установлены

### 2. Проверьте логи функций

В **Deployments → Latest → View Function Logs**:
- ✅ Нет ошибок при запуске
- ✅ Приложение инициализируется

### 3. Убедитесь что:

- ✅ Root Directory = `server` (КРИТИЧЕСКИ ВАЖНО!)
- ✅ Файл `server/api/index.ts` существует
- ✅ Файл `server/src/app.ts` существует
- ✅ Нет ошибок в логах

---

## 📝 Что изменилось

### До:
- `vercel.json` указывал на `src/app.ts`
- Проблемы с путями и маршрутизацией

### После:
- `vercel.json` указывает на `api/index.ts`
- `api/index.ts` импортирует и экспортирует Express app из `src/app.ts`
- Все запросы обрабатываются через один handler

---

## ✅ Чеклист

- [ ] Root Directory установлен на `server` в Vercel
- [ ] Файл `server/api/index.ts` создан
- [ ] `vercel.json` обновлен
- [ ] `tsconfig.json` обновлен
- [ ] Изменения отправлены на GitHub
- [ ] Проект пересобран на Vercel (Redeploy)
- [ ] Health check работает: `curl https://marshrutka.fun`

---

## 💡 Главное

**Root Directory ДОЛЖЕН быть установлен на `server`!**

Без этого Vercel не найдет файлы и будет возвращать 404.

После установки Root Directory и пересборки проекта все должно заработать!

---

**🎯 После исправления Root Directory пересоберите проект и проверьте что все работает!**

